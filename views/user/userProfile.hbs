<h1>Profil uporabnika</h1>
<h2>Ime: </h2>
<h3>{{user.username}}</h3>
<h2>Mail:</h2>
<h3>{{user.email}}</h3>

<h2>Profile Picture</h2>
<img src="{{user.profilePicture}}" alt="Profile Picture" width="150">
<form action="/users/setPicture" method="POST" enctype="multipart/form-data">
    <input type="file" name="profilePicture" accept="image/*" required>
    <button type="submit">Upload</button>
</form>

<br><a type="button" href="logout">Odjava </a>

<h2>Statistika:</h2>
<ul>
    <p>Število odgovorov: {{numComments}}</p>
    <p>Število vprašanj: {{numQuestions}}</p>
    <p>Karma: {{totalScore}}</p>
    <p>Število sprejetih odgovorov: {{numAcceptedComments}}</p>
    <p>Povprečna ocena odgovorov: {{averageScore}}</p>
</ul>




<h2>Questions:</h2>
<ul>
    {{#each questions}}
    <li>
        <a href="/questions/{{this._id}}">{{this.title}}</a>
    </li>
    {{/each}}
</ul>
